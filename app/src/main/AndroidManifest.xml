<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datingapp2021">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatingApp2021">
        <service android:name=".logic.Service.MainService"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:label="@string/profile">
        </activity>
        <activity
            android:name=".ui.profile.OwnProfileActivity"
            android:label="@string/profile">
        </activity>
        <activity
            android:name=".ui.profile.SettingsActivity"
            android:label="@string/profile_settings">
        </activity>
        <activity
            android:name=".ui.profile.ProfileEditorActivity"
            android:label="@string/profile_editor">
        </activity>
        <activity
            android:name=".ui.chat.ChatActivity"
            android:label="@string/chat">
        </activity>
    </application>

</manifest>